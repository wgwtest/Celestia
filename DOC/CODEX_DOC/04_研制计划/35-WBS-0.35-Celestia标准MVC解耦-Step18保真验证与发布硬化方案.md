# Celestia MVC Step18 Fidelity Verification And Release Hardening Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a Step18 evidence path that expands automated fidelity checks after Step17 without overstating cross-process View3D parity.
**Architecture:** Step18 treats the Step17 `scene.frame`, `ResourceRef`, Model command output, and resource projection fields as frozen. The first deliverable is a dedicated Step18 regression mode that reuses ordinary SDL screenshot coverage, strengthens multi-process View3D runtime assertions, and writes a Step18 machine report. Later visual-fidelity tasks can use that report path as their acceptance gate.
**Tech Stack:** C++17, CMake, doctest, PowerShell, existing `tools/regression` harness, SDL unified exe screenshots, `RuntimeAssemblyRunner`, MVC scan scripts, Quick compatibility matrix.

---

## 1. Scope And Boundary

Step18 is allowed to expand verification, reporting, and View3D visual evidence. It is not allowed to modify frozen Model-facing output fields unless a focused failing test and compatibility regression evidence prove a bug fix.

Can say after Task 1 passes:

```text
There is a dedicated Step18 machine-report mode.
Step18 can prove ordinary SDL screenshot coverage and multi-process View3D runtime log coverage in one report.
Step18 report output is stored in the formal machine-record directory.
```

Cannot say after Task 1 alone:

```text
Cross-process View3D has historical renderer visual parity.
All historical Celestia visual features are covered.
Step18 has completed final release hardening.
```

## 2. File Responsibility Map

```text
tools/regression/run_celestia_compat_regression.ps1
  Add Step18 mode, Step18 report copy, and strengthened runtime smoke assertions.

tools/regression/helpers/Run-CelestiaCompatSelfTest.ps1
  Add static self-test checks that Step18 mode and helpers exist.

tools/regression/README.md
  Document the Step18 command and claim boundary.

DOC/CODEX_DOC/06_测试文档/01_验收大纲/02-Celestia-Step18跨进程View3D保真验收大纲.md
  Define the Step18 acceptance gate and claim limits.

DOC/CODEX_DOC/06_测试文档/03_机测记录/<timestamp>-Celestia-Step18-machine-report.md
  Machine report copied by Step18 mode.

DOC/CODEX_DOC/06_测试文档/04_结论报告/02-Celestia跨进程View3D能力保真阶段结论.md
  Human-facing conclusion updated only after evidence exists.

CODEX_START_HERE.md
  Record Step18 current status and latest report path.
```

## 3. Implementation Tasks

### Task 1: Step18 Regression Mode And Machine Report

**Files:**
- Modify: `tools/regression/helpers/Run-CelestiaCompatSelfTest.ps1`
- Modify: `tools/regression/run_celestia_compat_regression.ps1`
- Modify: `tools/regression/README.md`
- Create: `DOC/CODEX_DOC/06_测试文档/01_验收大纲/02-Celestia-Step18跨进程View3D保真验收大纲.md`
- Modify: `CODEX_START_HERE.md`
- Modify: `DOC/CODEX_DOC/04_研制计划/35-WBS-0.35-Celestia标准MVC解耦-Step18保真验证与发布硬化方案.md`

- [ ] **Step 1: Write the failing Step18 self-test**

Modify `tools/regression/helpers/Run-CelestiaCompatSelfTest.ps1` after the runner existence assertion:

```powershell
$runnerText = Get-Content -LiteralPath (Join-Path $regressionRoot "run_celestia_compat_regression.ps1") -Raw
if ($runnerText -notmatch 'ValidateSet\("SelfTest", "InitBaseline", "Quick", "Full", "Step18"\)') {
    throw "Regression runner does not expose Step18 mode"
}
foreach ($required in @("Invoke-Step18", "Copy-Step18ReportToDocs", "Get-RuntimeSmokeStatus")) {
    if ($runnerText -notmatch $required) {
        throw "Regression runner is missing Step18 helper: $required"
    }
}
```

- [ ] **Step 2: Run self-test and verify RED**

Run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\regression\run_celestia_compat_regression.ps1 -Mode SelfTest
```

Expected RED:

```text
Regression runner does not expose Step18 mode
```

- [ ] **Step 3: Add Step18 mode to the runner**

Modify the `param` block in `tools/regression/run_celestia_compat_regression.ps1`:

```powershell
[ValidateSet("SelfTest", "InitBaseline", "Quick", "Full", "Step18")]
[string] $Mode = "Quick",
```

Add these helpers after `Write-Report`:

```powershell
function Get-RuntimeSmokeStatus {
    param([object[]] $RuntimeSmoke)

    if (@($RuntimeSmoke | Where-Object { $_.Status -eq "fail" }).Count -gt 0) {
        return "fail"
    }
    if (@($RuntimeSmoke | Where-Object { $_.Status -eq "warn" }).Count -gt 0) {
        return "warn"
    }
    return "pass"
}

function Copy-Step18ReportToDocs {
    param(
        [object] $Context,
        [string] $ReportPath
    )

    $testDocDir = "06_" + (New-UnicodeText @(0x6D4B, 0x8BD5, 0x6587, 0x6863))
    $machineRecordDir = "03_" + (New-UnicodeText @(0x673A, 0x6D4B, 0x8BB0, 0x5F55))
    $docReportRoot = Join-Path (Join-Path (Join-Path $repoRoot "DOC\CODEX_DOC") $testDocDir) $machineRecordDir
    New-Directory $docReportRoot
    $docReport = Join-Path $docReportRoot ($Context.Timestamp + "-Celestia-Step18-machine-report.md")
    Copy-Item -LiteralPath $ReportPath -Destination $docReport -Force
    return $docReport
}
```

Add this function after `Invoke-Full`:

```powershell
function Invoke-Step18 {
    $context = New-RunContext "Step18"
    if (-not $SkipBuild) {
        Invoke-CMakeAndCTest -SourceRoot $repoRoot -BuildDir $CurrentBuildDir -LogRoot (Join-Path $context.LogRoot "current-build")
    }

    Invoke-MvcScans -LogRoot (Join-Path $context.LogRoot "mvc-scans")
    $runtimeSmoke = Invoke-RuntimeSmoke -BuildDir $CurrentBuildDir -RunRoot $context.RunRoot -RequireRuntimeDetails
    $screens = Invoke-ScreenshotSet -Label "current" -BuildDir $CurrentBuildDir -RunRoot $context.RunRoot
    Assert-NoResidualProcesses

    $status = Get-RuntimeSmokeStatus $runtimeSmoke
    if (@($screens | Where-Object { $_.Status -eq "fail" }).Count -gt 0) {
        $status = "fail"
    }

    $report = Write-Report -Context $context -RunMode "Step18" -CurrentScreens $screens -RuntimeSmoke $runtimeSmoke -ExtraStatus $status
    $docReport = Copy-Step18ReportToDocs -Context $context -ReportPath $report
    Write-Step "step18 report: $report"
    Write-Step "step18 doc report: $docReport"
}
```

Add the switch entry:

```powershell
"Step18" {
    Invoke-Step18
}
```

- [ ] **Step 4: Strengthen runtime smoke detail checks**

Modify `Invoke-RuntimeSmoke` signature:

```powershell
function Invoke-RuntimeSmoke {
    param(
        [string] $BuildDir,
        [string] $RunRoot,
        [switch] $RequireRuntimeDetails
    )
```

After `Invoke-ProcessWithTimeout`, read stdout and add assertions:

```powershell
$stdoutPath = Join-Path $logDir ($configName + ".stdout.log")
$stderrPath = Join-Path $logDir ($configName + ".stderr.log")
Invoke-ProcessWithTimeout `
    -FilePath $exe `
    -ArgumentList @("--runtime-config", $runtimeConfig) `
    -WorkingDirectory $logDir `
    -StdoutPath $stdoutPath `
    -StderrPath $stderrPath `
    -TimeoutSeconds 30

$status = "pass"
$detail = $runtimeConfig
if ($RequireRuntimeDetails) {
    $stdout = Get-Content -LiteralPath $stdoutPath -Raw
    foreach ($token in @("all hosts stopped")) {
        if ($stdout -notmatch [regex]::Escape($token)) {
            $status = "fail"
            $detail = "missing token: $token; log=$stdoutPath"
        }
    }
    if ($configName -match "3d") {
        foreach ($token in @("view.frameRendered count=", "view.frameRendered payload=", "bodyCount=", "resourceCount=")) {
            if ($stdout -notmatch [regex]::Escape($token)) {
                $status = "fail"
                $detail = "missing token: $token; log=$stdoutPath"
            }
        }
    }
}

$results.Add([pscustomobject]@{ Name = $configName; Status = $status; Detail = $detail })
```

Remove the old unconditional `$results.Add(...)` line for the same config.

- [ ] **Step 5: Verify GREEN for self-test**

Run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\regression\run_celestia_compat_regression.ps1 -Mode SelfTest
```

Expected:

```text
Celestia compatibility regression self-test passed
```

- [ ] **Step 6: Document Step18 command**

Add to `tools/regression/README.md` under Commands:

```markdown
Step18 cross-process View3D evidence gate:

```powershell
powershell -ExecutionPolicy Bypass -File tools\regression\run_celestia_compat_regression.ps1 -Mode Step18
```

`Step18` runs the current SDL screenshot matrix and strengthens multi-process View3D runtime-smoke log assertions. It writes a machine report under the artifact root and copies the report into `DOC/CODEX_DOC/06_测试文档/03_机测记录/`.
```

- [ ] **Step 7: Add Step18 acceptance outline**

Create `DOC/CODEX_DOC/06_测试文档/01_验收大纲/02-Celestia-Step18跨进程View3D保真验收大纲.md`:

```markdown
# Celestia Step18 Cross-Process View3D Fidelity Acceptance Outline

## Scope

This outline validates Step18 evidence only. It does not declare complete historical renderer parity.

## Required Machine Evidence

```text
Step18 regression mode passes.
Current SDL screenshot matrix passes.
Multi-process View3D runtime smoke logs contain view.frameRendered count and payload.
Runtime smoke logs contain bodyCount and resourceCount for 3D configs.
No residual Celestia runtime processes remain after the run.
MVC dependency scan passes.
MVC CMake target scan passes.
```

## Claim Boundary

Can say:

```text
Step18 has a machine-verifiable evidence gate for current SDL visuals and multi-process View3D real-scene runtime output.
```

Cannot say:

```text
Cross-process View3D has complete historical renderer visual parity.
Qt or Win32 frontend parity has been proven.
Every Celestia visual feature is covered.
```
```

- [ ] **Step 8: Run Step18 mode**

Run:

```powershell
cmd.exe /d /s /c "call ""C:\Program Files (x86)\Microsoft Visual Studio\18\BuildTools\Common7\Tools\VsDevCmd.bat"" -arch=x64 -host_arch=x64 >nul && ""C:\Program Files (x86)\Microsoft Visual Studio\18\BuildTools\Common7\IDE\CommonExtensions\Microsoft\CMake\CMake\bin\cmake.exe"" --build ""D:\WorkSpace\Codex\CeleNew\Celestia\build-mvc-sdl-rel"" --config Release --target celestia-sdl -- -k 0"
powershell -NoProfile -ExecutionPolicy Bypass -File tools\regression\run_celestia_compat_regression.ps1 -Mode Step18 -SkipBuild
```

Expected:

```text
step18 report: <artifact path>\machine-report.md
step18 doc report: DOC\CODEX_DOC\06_测试文档\03_机测记录\<timestamp>-Celestia-Step18-machine-report.md
8/8 current screenshots pass in the report
6/6 runtime smoke configs pass in the report
```

- [ ] **Step 9: Commit Task 1**

Run:

```powershell
git add tools/regression/run_celestia_compat_regression.ps1 tools/regression/helpers/Run-CelestiaCompatSelfTest.ps1 tools/regression/README.md DOC/CODEX_DOC/06_测试文档/01_验收大纲/02-Celestia-Step18跨进程View3D保真验收大纲.md CODEX_START_HERE.md DOC/CODEX_DOC/04_研制计划/35-WBS-0.35-Celestia标准MVC解耦-Step18保真验证与发布硬化方案.md
git commit -m "test: add Step18 View3D fidelity evidence gate"
```

### Task 2: Step18 Report Claim Boundary

**Files:**
- Modify: `tools/regression/run_celestia_compat_regression.ps1`
- Modify: `DOC/CODEX_DOC/06_测试文档/04_结论报告/02-Celestia跨进程View3D能力保真阶段结论.md`
- Modify: `CODEX_START_HERE.md`

- [ ] **Step 1: Write failing report-boundary self-test**

Extend `Run-CelestiaCompatSelfTest.ps1`:

```powershell
foreach ($required in @("Step18 Claim Boundary", "does not prove complete historical renderer parity")) {
    if ($runnerText -notmatch [regex]::Escape($required)) {
        throw "Step18 report boundary text is missing: $required"
    }
}
```

- [ ] **Step 2: Run self-test and verify RED**

Run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\regression\run_celestia_compat_regression.ps1 -Mode SelfTest
```

Expected RED:

```text
Step18 report boundary text is missing
```

- [ ] **Step 3: Add Step18-specific report boundary**

Modify `Write-Report()` so that claim-boundary text is selected by `$RunMode`:

```powershell
if ($RunMode -eq "Step18") {
    $lines.Add("## Step18 Claim Boundary")
    $lines.Add("")
    $lines.Add("A passing Step18 run supports only this claim:")
    $lines.Add("")
    $lines.Add("```text")
    $lines.Add("Step18 found no failure in the current SDL screenshot matrix or in the strengthened multi-process View3D runtime smoke checks covered by this report.")
    $lines.Add("```")
    $lines.Add("")
    $lines.Add("It does not prove complete historical renderer parity, pixel-perfect visual parity, or Qt/Win32 frontend parity.")
} else {
    $lines.Add("## Claim Boundary")
    $lines.Add("")
    $lines.Add('A passing `Full` run supports only this claim:')
    $lines.Add("")
    $lines.Add('```text')
    $lines.Add("No visible regression was found in the ordinary unified SDL exe / in-process main path compared with the fixed pre-MVC baseline covered by these scenes.")
    $lines.Add('```')
    $lines.Add("")
    $lines.Add("It does not prove exhaustive Celestia feature parity, pixel-perfect rendering, or Qt/Win32 frontend parity.")
}
```

Replace the existing generic claim-boundary block with this conditional block.

- [ ] **Step 4: Create the Step18 conclusion skeleton**

Create `DOC/CODEX_DOC/06_测试文档/04_结论报告/02-Celestia跨进程View3D能力保真阶段结论.md`:

```markdown
# Celestia Cross-Process View3D Fidelity Stage Conclusion

## Current Conclusion

Step18 evidence exists only after the Step18 regression mode has passed and a machine report is linked here.

## Can Say

```text
The Step18 evidence gate covers the current SDL screenshot matrix and strengthened multi-process View3D runtime-smoke output.
```

## Cannot Say

```text
Cross-process View3D has complete historical renderer visual parity.
Every visual feature in the original in-process renderer has been matched.
Qt and Win32 frontend parity has been validated.
```
```

- [ ] **Step 5: Verify and commit Task 2**

Run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\regression\run_celestia_compat_regression.ps1 -Mode SelfTest
powershell -NoProfile -ExecutionPolicy Bypass -File tools\regression\run_celestia_compat_regression.ps1 -Mode Step18 -SkipBuild
```

Commit:

```powershell
git add tools/regression/run_celestia_compat_regression.ps1 tools/regression/helpers/Run-CelestiaCompatSelfTest.ps1 DOC/CODEX_DOC/06_测试文档/04_结论报告/02-Celestia跨进程View3D能力保真阶段结论.md CODEX_START_HERE.md DOC/CODEX_DOC/04_研制计划/35-WBS-0.35-Celestia标准MVC解耦-Step18保真验证与发布硬化方案.md
git commit -m "docs: define Step18 View3D fidelity claim boundary"
```

### Task 3: Visual Scenario Expansion

**Files:**
- Create: `tools/regression/scenarios/09-selection-follow-goto.cel`
- Create: `tools/regression/scenarios/10-resource-fallback-missing.cel`
- Modify: `tools/regression/helpers/Run-CelestiaCompatSelfTest.ps1`
- Modify: `DOC/CODEX_DOC/06_测试文档/01_验收大纲/02-Celestia-Step18跨进程View3D保真验收大纲.md`

- [ ] **Step 1: Write failing scenario-count self-test**

Change the scenario count assertion to:

```powershell
if ($scenarios.Count -lt 10) {
    throw "Expected at least 10 CEL scenarios for Step18, found $($scenarios.Count)"
}
```

- [ ] **Step 2: Verify RED**

Run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\regression\run_celestia_compat_regression.ps1 -Mode SelfTest
```

Expected RED:

```text
Expected at least 10 CEL scenarios for Step18
```

- [ ] **Step 3: Add selection/follow/goto visual scenario**

Create `tools/regression/scenarios/09-selection-follow-goto.cel`:

```text
select { object "Sol/Earth" }
follow {}
goto { time 1.0 distance 8.0 }
wait { duration 2.0 }
print { text "Celestia compatibility regression: selection follow goto scene" origin "bottomleft" row -4 column 2 duration 5 }
capture { filename "$CAPTURE_FILE$" }
exit {}
```

- [ ] **Step 4: Add resource fallback/missing visual scenario**

Create `tools/regression/scenarios/10-resource-fallback-missing.cel`:

```text
select { object "Sol/Mars" }
goto { time 1.0 distance 8.0 }
renderflags { set "orbits|labels" }
wait { duration 2.0 }
print { text "Celestia compatibility regression: resource fallback coverage scene" origin "bottomleft" row -4 column 2 duration 5 }
capture { filename "$CAPTURE_FILE$" }
exit {}
```

- [ ] **Step 5: Verify and commit Task 3**

Run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\regression\run_celestia_compat_regression.ps1 -Mode SelfTest
powershell -NoProfile -ExecutionPolicy Bypass -File tools\regression\run_celestia_compat_regression.ps1 -Mode Step18 -SkipBuild
```

Commit:

```powershell
git add tools/regression/scenarios/09-selection-follow-goto.cel tools/regression/scenarios/10-resource-fallback-missing.cel tools/regression/helpers/Run-CelestiaCompatSelfTest.ps1 DOC/CODEX_DOC/06_测试文档/01_验收大纲/02-Celestia-Step18跨进程View3D保真验收大纲.md CODEX_START_HERE.md DOC/CODEX_DOC/04_研制计划/35-WBS-0.35-Celestia标准MVC解耦-Step18保真验证与发布硬化方案.md
git commit -m "test: expand Step18 visual scenario matrix"
```

### Task 4: Final Step18 Verification

**Files:**
- Modify: `CODEX_START_HERE.md`
- Modify: `DOC/CODEX_DOC/04_研制计划/35-WBS-0.35-Celestia标准MVC解耦-Step18保真验证与发布硬化方案.md`
- Modify: `DOC/CODEX_DOC/06_测试文档/04_结论报告/02-Celestia跨进程View3D能力保真阶段结论.md`

- [ ] **Step 1: Run focused checks**

Run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\regression\run_celestia_compat_regression.ps1 -Mode SelfTest
cmd.exe /d /s /c "call ""C:\Program Files (x86)\Microsoft Visual Studio\18\BuildTools\Common7\Tools\VsDevCmd.bat"" -arch=x64 -host_arch=x64 >nul && ""C:\Program Files (x86)\Microsoft Visual Studio\18\BuildTools\Common7\IDE\CommonExtensions\Microsoft\CMake\CMake\bin\ctest.exe"" --test-dir ""D:\WorkSpace\Codex\CeleNew\Celestia\build-mvc-sdl-rel"" -C Release -R ""Step12|Step13|Step14|Step15|Step16|Step17|SceneFrame|RealModel|Real Scene|View3D|Controller"" --output-on-failure"
```

- [ ] **Step 2: Run final screenshot/runtime evidence**

Run:

```powershell
cmd.exe /d /s /c "call ""C:\Program Files (x86)\Microsoft Visual Studio\18\BuildTools\Common7\Tools\VsDevCmd.bat"" -arch=x64 -host_arch=x64 >nul && ""C:\Program Files (x86)\Microsoft Visual Studio\18\BuildTools\Common7\IDE\CommonExtensions\Microsoft\CMake\CMake\bin\cmake.exe"" --build ""D:\WorkSpace\Codex\CeleNew\Celestia\build-mvc-sdl-rel"" --config Release --target celestia-sdl -- -k 0"
powershell -NoProfile -ExecutionPolicy Bypass -File tools\regression\run_celestia_compat_regression.ps1 -Mode Step18 -SkipBuild
powershell -NoProfile -ExecutionPolicy Bypass -File tools\regression\run_celestia_compat_regression.ps1 -Mode Quick -SkipBuild
```

- [ ] **Step 3: Run scans**

Run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\mvc\scan_mvc_dependencies.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File tools\mvc\scan_cmake_targets.ps1
powershell -NoProfile -Command "$term = [string]([char]0x5951) + [string]([char]0x7EA6); rg -n $term DOC src test tools CODEX_START_HERE.md -S"
git diff --check
```

- [ ] **Step 4: Record evidence and commit**

Update the plan, `CODEX_START_HERE.md`, and the Step18 conclusion with:

```text
SelfTest result
Step12-17 broad CTest count
Step18 report path
Quick report path
screenshot count
runtime smoke count
claim boundary
```

Commit:

```powershell
git add CODEX_START_HERE.md DOC/CODEX_DOC/04_研制计划/35-WBS-0.35-Celestia标准MVC解耦-Step18保真验证与发布硬化方案.md DOC/CODEX_DOC/06_测试文档/04_结论报告/02-Celestia跨进程View3D能力保真阶段结论.md
git commit -m "docs: record Step18 fidelity evidence"
```

## 4. Completion Criteria

Step18 is complete only when all conditions below are proven in the current worktree:

```text
Step18 regression mode exists and SelfTest covers it.
Step18 mode runs current SDL screenshot matrix.
Step18 mode strengthens multi-process View3D runtime-smoke log assertions.
Step18 mode writes a machine report into DOC/CODEX_DOC/06_测试文档/03_机测记录.
Step18 acceptance outline exists.
Step18 conclusion separates ordinary SDL preservation from cross-process View3D parity.
Step18 expanded visual scenario matrix passes.
Step12-17 broad CTest still passes.
MVC dependency and CMake scans pass.
Quick regression still passes.
```

## 5. Self-Review

Spec coverage:

```text
Regression matrix expansion: Tasks 1 and 3.
Multi-process View3D runtime proof: Task 1.
Stage conclusion: Task 2 and Task 4.
Screenshot evidence: Task 1, Task 3, and Task 4.
Release hardening checks: Task 4.
```

Placeholder scan:

```text
No unfilled markers.
No unspecified commands.
No task without concrete files.
```

Type and script consistency:

```text
Step18 mode is added before it is called.
Copy-Step18ReportToDocs is introduced before SelfTest expects it.
Get-RuntimeSmokeStatus is introduced before Step18 status aggregation uses it.
RequireRuntimeDetails is optional, so Quick and Full behavior remains compatible.
```
