# Celestia Step24 前 MVC 架构检查点机测记录

- 记录时间: 2026-07-15 10:14:05
- 测试分支: `codex/celestia-mvc-step13-real-model-backend`
- 测试提交: `0f733ef603b8db5ffc02b735c153d8302bfad3ed`

## 1. 记录定位

本记录是 `02-14-Celestia-Step24前MVC架构检查点报告.md` 的机器验证附件。本轮未修改 C++、CMake、测试脚本或运行时配置。

验证目标:

1. 确认当前精确提交可编译。
2. 确认当前全量 CTest 状态。
3. 重新采集 MVC 边界扫描数据。
4. 确认统一 SDL exe 相对固定基线的 10 场景对比结果。
5. 检查当前 Model Host 可执行文件的动态导入。

## 2. 构建验证

执行目标:

```text
unit
celestia-model-host
celestia-controller-host
celestia-view3d-host
celestia-sdl
```

执行命令:

```powershell
cmake --build build-mvc-sdl-rel --config Release `
  --target unit celestia-model-host celestia-controller-host celestia-view3d-host celestia-sdl `
  --parallel 8
```

结果: `pass`

构建输出包含 `CopyDependentDlls.cmake` 的 CMake policy `CMP0207` developer warning，未导致目标失败。

## 3. 全量 CTest

命令:

```powershell
ctest --test-dir build-mvc-sdl-rel -C Release --output-on-failure
```

结果:

```text
100% tests passed, 0 tests failed out of 200
Total Test time (real) = 57.93 sec
```

## 4. MVC 静态扫描

### 4.1 常规依赖扫描

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\mvc\scan_mvc_dependencies.ps1
```

结果:

```text
MVC dependency scan passed
```

### 4.2 CMake 目标扫描

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\mvc\scan_cmake_targets.ps1
```

结果:

```text
MVC CMake target scan passed
```

### 4.3 边界债务扫描

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\mvc\scan_mvc_boundary_debt.ps1
```

结果:

```text
MVC boundary debt scan report: 53 finding(s)

adapter->runtime: 1
adapter->view: 4
runtime-model->app: 4
runtime-model-projection: 44
runtime-model->view: 0
model->adapter: 0
model->view: 0
model-view-symbol: 0
```

### 4.4 Model 直接 Adapter 边界

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File test\scripts\test_mvc_model_adapter_boundary_clean.ps1
```

结果:

```text
MVC model adapter boundary is clean
```

## 5. Model Host PE 动态依赖

命令:

```powershell
dumpbin /dependents build-mvc-sdl-rel\src\celruntime\celestia-model-host.exe
```

非系统主要动态依赖:

```text
fmt.dll
libpng16.dll
jpeg62.dll
```

当前 PE 导入表未出现 SDL、OpenGL 或 libepoxy。这只证明当前 Release 链接结果中未留下这些动态导入；不能否定 `celestia-model-host` 的 CMake 源码对象集仍包含 `celestia_view_adapter`、`celestia_view3d` 和 `celrender` 的构建图事实。

## 6. Full 基线对比

命令:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass `
  -File tools\regression\run_celestia_compat_regression.ps1 `
  -Mode Full -SkipBuild
```

工件报告:

```text
D:\WorkSpace\Codex\CeleNew\.regression-artifacts\Celestia\runs\2026-07-14-134535-0f733ef-full\machine-report.md
```

正式报告:

```text
DOC/CODEX_DOC/06_测试文档/03_机测记录/2026-07-14-134535-Celestia-compat-regression-machine-report.md
```

结果:

```text
status: pass
current commit: 0f733ef603b8db5ffc02b735c153d8302bfad3ed
baseline commit: 44ec265659d2aa666cbf7546e36e4dde471d54ba
current screenshots: 10/10 pass
baseline/current comparisons: 10/10 pass
runtime smoke configs: 6/6 pass
residual Celestia processes: none
```

图像对比摘要:

| 场景 | dHash Hamming | Average Color Distance | 结果 |
|---|---:|---:|---|
| 01 Earth default | 0 | 0.001 | pass |
| 02 Earth clouds/orbits/labels | 0 | 0.001 | pass |
| 03 Moon close | 0 | 0.000 | pass |
| 04 Saturn rings | 0 | 0.000 | pass |
| 05 Asteroid/spacecraft | 0 | 0.000 | pass |
| 06 Starfield/constellations | 5 | 0.002 | pass |
| 07 Galaxy/deep sky | 0 | 0.000 | pass |
| 08 Script overlay/HUD | 0 | 0.000 | pass |
| 09 Selection/follow/goto | 0 | 0.001 | pass |
| 10 Resource fallback/missing | 0 | 0.003 | pass |

已人工检查 Full contact sheet，未发现统一 SDL exe 主路径的明显可见退化。

## 7. 验证边界

本记录能够支持:

1. 当前精确提交的指定构建目标成功。
2. 现有 200 项 CTest 全部通过。
3. 当前扫描基线为 53 项。
4. 统一 SDL exe 在已覆盖的 10 场景中与固定基线高度一致。

本记录不能支持:

1. Model Host 已完成物理编译解耦。
2. Controller 已驱动真实 `Simulation/Observer` 全部业务状态。
3. 跨进程 View3D 已达到原 View3D 视觉或功能等价。
4. 当前 MVC 静态扫描规则已覆盖完整物理依赖图。
