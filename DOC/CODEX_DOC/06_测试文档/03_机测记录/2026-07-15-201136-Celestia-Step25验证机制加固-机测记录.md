# Celestia Step25 验证机制加固机测记录

- 记录时间：`2026-07-15 20:11:36 +08:00`
- 工作目录：`D:\WorkSpace\Codex\CeleNew\Celestia`
- 分支：`codex/celestia-mvc-step13-real-model-backend`
- 测试提交：`0f733ef603b8db5ffc02b735c153d8302bfad3ed`
- 提交标题：`refactor: separate resource path indexes from View3D`
- 工作树：`dirty`

工作树中的变更包含既有 Step24 文档、Step25 计划/验收文档和本轮验证脚本。本轮相对测试提交没有修改 `src/`、`test/unit/` 或根 `CMakeLists.txt`。本记录只证明当前工作树上的验证机制及其实际运行结果，不把未提交状态描述为远端成果。

## 1. 验证结论

Step25 的九项任务已在当前工作树完成，最终状态为：

```text
SelfTest: pass / exit 0
FI-00..FI-11: 12/12 expected status and exit code matched
Quick: pass / exit 0 / 60 checks / 0 non-pass
Full: pass / exit 0 / 96 checks / 0 non-pass
Step18: pass / exit 0 / 60 checks / 0 non-pass
Full contact sheet: 20 entries / 10 baseline-current pairs / manually reviewed
residual Celestia processes: none
```

该结论证明验证门槛能够可靠通过、可靠失败、可靠报告，并且固定统一程序基线没有被 Full 模式静默重建。它不证明 Model 已达到 `M-L1`，不证明真实命令闭环，不证明跨进程 View3D 已恢复原 3D 业务画面，也不代表 `VG-01..VG-16` 已关闭。

## 2. SelfTest

命令：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass `
  -File tools\regression\run_celestia_compat_regression.ps1 `
  -Mode SelfTest
```

实际结果：`pass`，exit code `0`，耗时 `5.5 s`。

```text
runtime checkpoint fixture: pass
runtime two-image fixture: pass
verification matrix: 10 unified exe scenarios, 6 runtime scenarios
image_metrics self-test passed
Celestia compatibility regression self-test passed
```

SelfTest 覆盖统一结果对象、`pass/warn/fail/error/skipped` 聚合、`0/1/2/3` 退出码、矩阵精确集合、路径越界、重复 ID/checkpoint/artifact、Runtime 多检查点和两图 fixture。

## 3. 故障注入

命令：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass `
  -File tools\regression\helpers\Run-CelestiaCompatFaultInjection.ps1
```

最终结果：`12/12` 用例的 expected/actual 状态和退出码一致，套件 exit code `0`，耗时 `26 s`。

| 用例 | 预期状态/退出码 | 实际状态/退出码 | 结果 |
|---|---|---|---|
| `FI-00` | `pass/0` | `pass/0` | pass |
| `FI-01` | `fail/1` | `fail/1` | pass |
| `FI-02` | `fail/1` | `fail/1` | pass |
| `FI-03` | `fail/1` | `fail/1` | pass |
| `FI-04` | `fail/1` | `fail/1` | pass |
| `FI-05` | `fail/1` | `fail/1` | pass |
| `FI-06` | `fail/1` | `fail/1` | pass |
| `FI-07` | `fail/1` | `fail/1` | pass |
| `FI-08` | `fail/1` | `fail/1` | pass |
| `FI-09` | `warn/2` | `warn/2` | pass |
| `FI-10` | `error/3` | `error/3` | pass |
| `FI-11` | `fail/1` | `fail/1` | pass |

每个预期失败用例之后都运行干净变体并得到 `pass/0`，排除了用例间污染。另用临时反向 fixture 把 `FI-06` 的预期退出码从 `1` 改为 `0`，故障套件自身正确返回 `1`，并报告：

```text
FI-06 expected/actual mismatch: expected=fail/0 actual=fail/1 reportExit=1
```

这证明套件不会在 expected/actual 不一致时给出伪通过。

## 4. 固定基线清单

清单：

```text
D:\WorkSpace\Codex\CeleNew\.regression-artifacts\Celestia\baselines\44ec265\baseline-manifest.json
```

实际校验：

```text
baseline commit: 44ec265659d2aa666cbf7546e36e4dde471d54ba
manifest image entries: 10
PNG files on disk: 10
missing files: 0
SHA256 mismatches: 0
duplicate scenario/checkpoint entries: 0
```

在隔离的损坏基线副本上删除一张图片后执行 Full，实际得到 `fail/1`，缺失文件在运行后仍然缺失。这证明 Full 不会自动修补或重建固定基线。正式 `InitBaseline` 运行报告为：

```text
D:\WorkSpace\Codex\CeleNew\.regression-artifacts\Celestia\runs\2026-07-15-181000-0f733ef-initbaseline\machine-report.json
```

## 5. 正式运行

三个正式模式均没有使用 `-SkipBuild`。

| 模式 | Run ID | 检查总数 | 非通过 | 状态/退出码 |
|---|---|---:|---:|---|
| Quick | `2026-07-15-193233-0f733ef-quick` | 60 | 0 | `pass/0` |
| Full | `2026-07-15-193548-0f733ef-full` | 96 | 0 | `pass/0` |
| Step18 | `2026-07-15-200722-0f733ef-step18` | 60 | 0 | `pass/0` |

Quick 报告：

```text
D:\WorkSpace\Codex\CeleNew\.regression-artifacts\Celestia\runs\2026-07-15-193233-0f733ef-quick\machine-report.json
D:\WorkSpace\Codex\CeleNew\.regression-artifacts\Celestia\runs\2026-07-15-193233-0f733ef-quick\machine-report.md
```

Full 报告：

```text
D:\WorkSpace\Codex\CeleNew\.regression-artifacts\Celestia\runs\2026-07-15-193548-0f733ef-full\machine-report.json
D:\WorkSpace\Codex\CeleNew\.regression-artifacts\Celestia\runs\2026-07-15-193548-0f733ef-full\machine-report.md
```

Step18 报告：

```text
D:\WorkSpace\Codex\CeleNew\.regression-artifacts\Celestia\runs\2026-07-15-200722-0f733ef-step18\machine-report.json
D:\WorkSpace\Codex\CeleNew\.regression-artifacts\Celestia\runs\2026-07-15-200722-0f733ef-step18\machine-report.md
```

Full 中的构建和静态检查实际结果：

```text
CTest: 200/200 passed, 0 failed, 55.17 s
MVC dependency scan: pass
MVC CMake target scan: pass
Model direct Adapter boundary check: pass
```

## 6. 统一程序 10 场景

Full 中每个场景的 baseline image、current image 和 comparison 都是 required 检查，三列均为 `pass`。

| 场景 | Baseline | Current | Comparison | dHash | 平均颜色距离 |
|---|---|---|---|---:|---:|
| `01-earth-default` | pass | pass | pass | 0 | 0.000 |
| `02-earth-clouds-orbits-labels` | pass | pass | pass | 0 | 0.001 |
| `03-moon-close` | pass | pass | pass | 0 | 0.000 |
| `04-saturn-rings` | pass | pass | pass | 0 | 0.000 |
| `05-asteroid-or-spacecraft` | pass | pass | pass | 0 | 0.000 |
| `06-starfield-constellations` | pass | pass | pass | 8 | 0.001 |
| `07-galaxy-deepsky` | pass | pass | pass | 0 | 0.000 |
| `08-script-overlay-hud` | pass | pass | pass | 0 | 0.000 |
| `09-selection-follow-goto` | pass | pass | pass | 0 | 0.001 |
| `10-resource-fallback-missing` | pass | pass | pass | 0 | 0.002 |

这些名称不扩大场景含义：`05` 实际只选择 Eros，`08` 只覆盖文本/HUD，`09` 不证明持续 follow 或完整 journey，`10` 没有制造真实资源缺失。该边界已写入验证矩阵的 `knownGaps`。

## 7. Runtime 6 场景与 28 个检查点

六个子进程场景均为 `process pass/exit 0`，28 个 required checkpoint 全部为 `pass`。

| 场景 | 检查点数 | 清洁退出 | Trace | 关键业务断言 |
|---|---:|---|---|---|
| `runtime-2d-stdio` | 2 | pass | pass | 仅生命周期/stdio；不声明业务画面 |
| `runtime-2d-local-socket` | 2 | pass | pass | 仅生命周期/local-socket；不声明业务画面 |
| `runtime-3d-stdio` | 8 | pass | pass | frame count > 0；synthetic Earth identity 24 次；body=1、star=1、resource=3、missingRequired=0 |
| `runtime-3d-local-socket` | 8 | pass | pass | frame count > 0；synthetic Earth identity 24 次；body=1、star=1、resource=3、missingRequired=0 |
| `runtime-switch-2d-to-3d-local-socket` | 4 | pass | pass | 3D frame count > 0；synthetic Earth identity 32 次 |
| `runtime-switch-3d-to-2d-local-socket` | 4 | pass | pass | 3D frame count > 0；synthetic Earth identity 16 次 |

Runtime 里的 Earth 身份是 synthetic Model fixture，不是真实 Model 场景，也没有产生原 Celestia 3D 业务画面。它证明的是进程、传输、消息和检查点机制。

## 8. 接触图人工检查

工件：

```text
D:\WorkSpace\Codex\CeleNew\.regression-artifacts\Celestia\runs\2026-07-15-193548-0f733ef-full\contact-sheet.png
D:\WorkSpace\Codex\CeleNew\.regression-artifacts\Celestia\runs\2026-07-15-193548-0f733ef-full\contact-sheet.json
```

元数据：`20` entries，`10` baseline/current pairs，尺寸 `380 x 4630`。

| 场景 | 人工逐项结论 |
|---|---|
| `01` | 星场和 HUD 构图配对一致，未见缺图或明显位置漂移 |
| `02` | 多条轨道、标签和星场结构配对一致 |
| `03` | 月球大小、表面纹理、位置和背景配对一致 |
| `04` | 土星及环的右侧构图、尺度和纹理配对一致 |
| `05` | Eros 场景的星场、HUD 和目标构图配对一致 |
| `06` | 星座线、星座标签和星场分布配对一致 |
| `07` | 深空场景的暗背景、HUD 和可见点状结构配对一致 |
| `08` | 文本/HUD 与星场位置配对一致 |
| `09` | 轨道、标签、说明文字和星场构图配对一致 |
| `10` | Mars 正常资源场景的轨道、标签和说明文字配对一致 |

人工结论仅适用于这 10 个最终截图检查点，不证明中间状态、隐藏状态或完整交互链。

## 9. 残留进程与执行异常

正式 Quick、Full、Step18 报告中的 `runtime/no-residual-processes` 均为 `pass`；最终独立检查没有发现 `celestia`、`celestia-sdl` 或相关 host 残留进程。

首次基线构建发现旧 CMake cache 指向已经不存在的 MSVC `14.50.35717`，当前工具链为 `14.51.36231`。runner 已增加缓存编译器存在性检查，随后重新配置并完成正式运行。首次长重建超出外层 20 分钟等待窗口，确认其唯一残留构建进程没有子进程后终止；后续正式 InitBaseline、Quick、Full 和 Step18 均独立通过。

## 10. 未关闭验证缺口

`02-16-Celestia原业务链与MVC责任归属基线.md` 定义的以下场景缺口仍全部开放：

```text
VG-01 时间状态机和多 Observer 同步
VG-02 Observer 与 View lens 分离
VG-03 Selection/ObjectRef/Picking/Search 闭环
VG-04 完整导航模式
VG-05 三类 Catalog 与 addon 生命周期
VG-06 Star 双路径和 AutoMag
VG-07 DSO 子类完整覆盖
VG-08 Body shape 与 CPU/GPU geometry
VG-09 Surface/Atmosphere/Ring 字段矩阵
VG-10 Orbit 事实到曲线
VG-11 Marker/ReferenceMark/Annotation
VG-12 per-View policy 平权
VG-13 Legacy/Lua 命令等价
VG-14 HUD/feedback/overlay/capture
VG-15 角色构建和进程隔离
VG-16 真实资源缺失/损坏/fallback
```

Step25 的目标是把验证工具本身加固为后续切片可依赖的门槛，不是在本阶段实现上述 16 个业务场景。

## 11. 提交状态与暂停点

当前成果尚未提交、尚未推送。下一候选工作是单独制定 `C-01` 时间/暂停权威状态实施计划；未经用户确认，不开始 `C-01`，不开始 View3D 迁移，不合并，不推送。
